//Write a palindrome function to check if the given number is palindrome or not.
//Use recursive strategy to check.
//Test your fibonacci function using the following testing code.


#include <assert.h>
#include<iostream>


bool palindrome(int num) {
    
}

int main() {
    int result = 0;
    bool palin = true;
    result = palindrome(12321);
    assert(palin);

    result = palindrome(102);
    assert(!palin);
}
